{
  "name": "trustlc-mock-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Mock customs & shipment portal for TrustLC. Simulates the real-world data source that the zkTLS attestation layer proves against.",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "node scripts/reset-data.js",
    "test": "node --test \"test/**/*.test.js\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.3.0"
  }
}
